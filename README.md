# adaptive-file-zipper
An optimized file compression technique that uses the entropy value for the input data and applies the best compression method (Run-Length Encoding, Block Huffman, etc.,) to improve efficiency over the traditional Huffman method.
